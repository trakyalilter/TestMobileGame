

========== COMBAT_DRIVE ==========
COMBAT DRIVE â€” `_run_boss_fight(zid, boss_id, length, p, N, farm_to=1)`, small-DT only (combat does NOT resolve under large delta: one shot/tick, overshoot discarded; win/lose edge-triggered in _execute_player_attack/enemy attack). DT=0.25.\n\nPRE-CHECK BEFORE THE LOOP (set_target_enemy bails BEFORE setting in_combat if hull<=0 or unpowered â€” verified combat_manager.gd:1183-1193 â€” but start_expedition spawns a RANDOM enemy and sets in_combat=true FIRST, so a naive fight runs vs trash and never increments boss_kills -> false dps wall):\n```\nvar sm = GameState.shipyard_manager\nif sm.current_hp <= 0 and not cm.in_combat: sm.repair_hull()\nif sm.energy_used > sm.energy_capacity: return \"POWER_WALL\"   # classify, don't fight\nif not _combat_ready(N): return \"NOT_READY\"\n```\n\nLOOP:\n```\ncm = GameState.combat_manager\n_set_active(cm)                          # stop previous active_manager.stop_action() THEN GameState.active_manager = cm\ncm.start_expedition(zid)                 # validates research_req + current_hp>0; sets in_combat; spawns RANDOM enemy\ncm.set_target_enemy(boss_id)             # PINS boss; re-checks power+hp and may BAIL silently\nif cm.current_enemy == null or String(cm.current_enemy.get(\"id\",\"\")) != boss_id:\n    cm.retreat(); return \"POWER_WALL\"    # set_target_enemy bailed -> power/hp wall, not a dps wall\nvar start_kills = int(cm.boss_kills.get(boss_id, 0))\nvar steps = int(length / DT); var s = 0; var deaths = 0\nwhile s < steps:\n    GameState.infrastructure_manager.process_tick(DT)\n    GameState.bounty_manager.process_tick(DT)\n    GameState.warp_manager.process_charge(DT)\n    if not cm.in_combat:                 # died/retreated (lose_fight -> retreat -> in_combat=false)\n        deaths += 1\n        if deaths > MAX_DEATHS_PER_FIGHT or not _combat_ready(N): break   # bail to regear\n        if sm.current_hp <= 0: sm.repair_hull()\n        p._arm_for_zone(N); p._ensure_powered(N); p._ensure_ammo(N)       # death can destroy 1/6 modules + clear ammo\n        cm.start_expedition(zid); cm.set_target_enemy(boss_id)\n        if String(cm.current_enemy.get(\"id\",\"\")) != boss_id: cm.retreat(); return \"POWER_WALL\"\n    cm.process_tick(DT)\n    sim_s += DT; step += 1\n    var k = int(cm.boss_kills.get(boss_id, 0))\n    if k > start_kills:                   # WIN: boss down (boss_kills increments in win_fight, combat_manager.gd:2398)\n        p.cleared[N if zid==ZONE_KEY[N] else _tier_of(zid)] = true   # mark clear ONCE on first delta\n        # for a clear kill farm_to==1 -> retreat; for the boss-core FARM branch keep going until enough cores\n        if int(GameState.resources.get_element_amount(\"Z%d_Core\"%_tier_of(zid))) >= farm_to:\n            cm.retreat(); return \"CLEARED\"\n        start_kills = k                   # win_fight auto-respawned the pinned target (spawn_enemy l.1210); keep farming\n    s += 1\nreturn \"TIMEOUT\"   # -> watchdog classifies the wall (dps/power/ammo)\n```\nWIN = `cm.boss_kills[boss_id]` delta. Zone N cleared <=> first delta on its boss; Z10 additionally sets `game_settings.z10_cleared`. NEXT enemy auto-spawns inside win_fight while in_combat stays true â€” never respawn manually unless retreated/died. ALWAYS `cm.retreat()` before returning or before any offline jump (TIMEOUT/POWER_WALL paths too) so in_combat=false and a later offline jump can't sweep trash.\n\nDAMAGE-TYPE pick is DATA-DRIVEN (do NOT hard-code a table; the prose table had a Z5 error). `_arm_for_zone(N)` reads the boss's enemy_db entry and picks `weakest = argmin(resist_k, resist_e, resist_x)`, mapping kinetic->z{N}_kinetic, energy->z{N}_energy, explosive->z{N}_MISSILE (the explosive weapon id is `z{N}_missile`, NOT z{N}_explosive â€” verified shipyard_manager.gd:651). VERIFIED boss resists: Z1 resist_x=-0.30, Z2 -0.40, Z5 resist_x=-0.40 (weak EXPLOSIVE, NOT energy), Z10 resist_k=-0.40 (kinetic). So the bot wants MISSILE on the VERY FIRST boss -> explosive ammo plumbing is load-bearing, not optional.\n\nAMMO IS LOAD-BEARING (verified: a non-cryo weapon with no ammo in ammo_loadout[slot] fires for 0 damage silently, combat_manager.gd:1738-1743; auto-equip only sets SlugT1/CellT1/literal-\"missile\" but the real explosive ammo is MissileT1, shipyard_manager.gd:1830-1837). `_ensure_ammo(N)` MUST, for every equipped non-cryo weapon slot: (a) ensure the matched ammo element is stocked (kinetic->SlugT1, energy->CellT1, explosive->MissileT1 via craft_missile_t1 which needs `combustion` research â€” verified processing_manager.gd:556-564), and (b) call `sm.set_slot_ammo(slot_idx, ammo_id)` so ammo_loadout[slot] is set (is_ammo_compatible accepts \"Missile\" in id, shipyard_manager.gd:2501-2509). FALLBACK: if explosive is decisively best but MissileT1/combustion is not yet attainable, `_arm_for_zone` falls back to the best ammo-supported type (kinetic Z1/Z2 are clearable with kinetic despite the resist) and the watchdog logs the substitution. `_combat_ready(N)` verifies is_armed() AND energy_used<=energy_capacity AND every non-cryo weapon slot has a compatible entry in ammo_loadout with stock>0 (not merely \"some ammo element exists\"). EQUIP resolves slot INDEX, not slot_type: build a slot_type->index map over `sm.hulls[sm.active_hull].slots` and pass the index to `equip_module(idx, module_id)`. UNDER-GEARED DEFEAT: handle_module_defeat destroys 1/6 of equipped modules + 10-50% durability on death; re-arm after EVERY death and bail to regear if `_combat_ready` goes false.

========== WARP_SCENARIOS ==========
WARP DRIVERS â€” one harness, a `target_warps` count (A=0, B=1, C=2) and a branch-aware spend. All three target the SAME goal (Z10 boss cleared) so the comparison is meaningful: A = single climb; B/C = climb, warp, re-climb faster under compounding mults.\n\nTRIGGER (B/C): `func _should_warp() -> bool: return wm.total_warps < target_warps and wm.calculate_warp_gains() >= 1 and _has_fought()`. score = (lifetime_credits - credits_at_warp_start) + buildings*1000 >= 500k (350k with REC_3); shards = floor(log2(score/thr))+1. `_has_fought()` = any boss_kills>0 this climb (guards the prestige-loop trap). Deterministic moment: warp the FIRST time both (warp-able) AND (>=1 zone cleared this climb) hold.\n\nEXECUTE + SPEND â€” BRANCH-AWARE (CRITICAL, verified BRANCH_REVEAL_WARP={engineering:1, combat:2, recursion:2}, warp_manager.gd:87; combat AND recursion only reveal at total_warps>=2; can_purchase_node's is_branch_revealed refuses them otherwise, warp_manager.gd:249,294). Scenario B's single warp spends ENGINEERING ONLY â€” NO Blueprint Cache, NO combat node. Drive every buy through can_purchase_node in a retry-until-false loop:\n```\nif _should_warp():\n    log_warp_event(\"PRE\", N, lifetime_credits, buildings, wm.calculate_warp_gains())\n    wm.execute_warp()       # +shards, total_warps++, WIPES resources+buildings+module_inventory+loadout, decays XP(keep ~30%),\n                            #   research+tree+shards PERSIST, re-seeds starter batteries as INVENTORY (no weapon equipped), calls save_game()\n    if wm.total_warps == 1:                 # Scenario B: engineering only revealed\n        buy_until_false([\"ENG_1\", \"ENG_4\", \"ENG_S1\", \"ENG_S2\"])\n    else:                                   # Scenario C (>=2 warps): combat+recursion revealed\n        buy_until_false([\"REC_1\", \"CMB_1\", \"CMB_2\", \"ENG_1\", \"ENG_4\", \"CMB_S1\", \"ENG_S1\", \"ENG_S2\"])\n    # buy_until_false: for id: while can_purchase_node(id) and shards: purchase_node(id)\n    log_warp_event(\"POST\", wm.warp_shards, wm.get_warp_tier(), wm.get_production_multiplier(), wm.get_combat_multiplier())\n    p.cleared.clear()       # per-run clear state resets â€” bot re-fights Z1->Z10 (faster, compounding Ã—2^tier)\n    p._force_fresh_rearm = true   # loadout wiped: next manage_meta arms from scratch, _combat_ready false until armed\n```\nNode ids verified real+implemented: ENG_1/ENG_4/ENG_S1/ENG_S2/CMB_1/CMB_2/CMB_S1/REC_1 (warp_manager.gd TREE_NODES; prereqs via internal `prereq:[...]` check).\n\nRESUME: research PERSISTS (warp soft_reset keeps unlocked_techs) so zone_N_access stay unlocked â€” zones ENTERABLE post-warp; but loadout AND module_inventory are wiped (starter batteries return as inventory, NOTHING equipped, is_armed() false). Bot re-arms from scratch and re-fights all bosses (cores were resources, gone). REPORT HEADLINE: B/C per-zone re-climb times should be materially shorter than A's first pass (deltas_vs_A) â€” that delta is the scenario result. For C, the 2nd warp fires the next time `_should_warp()` holds after the post-warp-1 rebuild reaches warp-able again, deepening the same combat/engineering spines.

========== INSTRUMENTATION ==========
INSTRUMENTATION â€” reuse `scripts/sim/sim_telemetry.gd` (JSONL writer) for machine-readable + mirror to print() for human-readable. Three record kinds:\n\n1) PER-ZONE-CLEAR (emitted in _run_boss_fight on CLEARED):\n```\n{\"evt\":\"zone_clear\",\"scenario\":\"A\",\"zone\":N,\"boss\":boss_id,\n \"sim_s\": round(sim_s), \"steps\": step, \"fight_steps\": s,\n \"core_farm_kills\": kills_for_this_gate,        # 1 for clear; 2..10 when re-farmed for next zone's research\n \"credits\": res.get_currency(\"credits\"), \"lifetime_credits\": res.lifetime_credits,\n \"g_lvl\": gathering.get_level(), \"p_lvl\": processing.get_level(),\n \"research_count\": research.unlocked_techs.size(),\n \"hull\": shipyard.active_hull, \"atk_k\": shipyard.attack_kinetic, \"atk_e\": shipyard.attack_energy,\n \"atk_x\": shipyard.attack_explosive, \"max_hp\": shipyard.max_hp,\n \"energy_used\": shipyard.energy_used, \"energy_cap\": shipyard.energy_capacity,\n \"warps\": wm.total_warps, \"shards\": wm.warp_shards, \"tier\": wm.get_warp_tier(),\n \"gated_by\": gate_reason}   # \"research:zone_N_access(mat:Steel x123)\" | \"research:credits(x45000)\" |\n                            #   \"research:Z{N-1}_Core x{q}\" | \"gear:dps_wall(...)\" | \"power:deficit\" |\n                            #   \"ammo:MissileT1=0\" | \"none\"\n```\nHuman line: `[CLEAR] %s Z%d %s @ sim=%.2fh steps=%d farmK=%d | cr=%s lifeCr=%s | g%d p%d | hull=%s atk=%.0f hp=%.0f | warps=%d shards=%.0f | gate=%s`.\n\n2) WARP EVENT: `{\"evt\":\"warp\",\"scenario\",\"phase\":\"PRE|POST\",\"total_warps\",\"shards\",\"tier\",\"prod_mult\",\"cmb_mult\",\"gath_mult\",\"xp_mult\",\"nodes_bought\":[...],\"score\":..,\"gains\":..}` + a print line.\n\n3) FINAL PER-SCENARIO SUMMARY (end of scenario):\n```\n{\"evt\":\"summary\",\"scenario\",\"result\":\"Z10_CLEARED|STALLED\",\"total_sim_s\",\"total_steps\",\n \"zone_clear_times_s\":[t1..t10], \"zone_clear_steps\":[s1..s10], \"core_farm_per_zone\":[..],\n \"deltas_vs_A\":[..],   # for B/C: per-zone sim-time speedup vs scenario A (the headline result)\n \"final_g_lvl\",\"final_p_lvl\",\"final_research_count\",\"final_lifetime_credits\",\n \"warps\",\"shards\",\"warp_tier\",\"stall\": stall_record_or_null}\n```\nPrint a compact table: one row per zone (Z, sim-time, steps, core-farm kills, binding gate), then the summary line. HEARTBEAT every K steps: `[SIM] scen=A N=4 | sim=%.2fh step=%d | g%d p%d | lifeCr=%s | last=%s`. All sim_s = sum of DT*steps across active/combat loops + offline gap seconds.

========== WATCHDOG ==========
WATCHDOG â€” stall = a budget of steps spent toward zone N with NO progress on anything that unblocks its boss. Track a per-zone window and a global cap; report a STALL as a structured FINDING (where + why), never crash.\n```\n# progress markers refreshed each step:\nprog = {cleared_count: cleared.size(), g_lvl, p_lvl, research_count, credits_floor,\n        core_count: sum(Z*_Core on hand), combat_ready_bit}\nMAX_STEPS_PER_ZONE = 200_000\nHARD_GLOBAL_CAP    = 4_000_000\nNO_PROGRESS_WINDOW = 40_000\nif step - last_progress_step > NO_PROGRESS_WINDOW or zone_steps[N] > MAX_STEPS_PER_ZONE:\n    reason = _classify_stall(N)   # greedy \"what blocks the next boss\" taxonomy:\n    #  - research:material  -> zone_N_access short a refined mat+qty (incl Ã—MATERIAL_MULTIPLIER)\n    #  - research:credits   -> zone_N_access short credits+amt (Ã—COST_MULTIPLIER)\n    #  - research:boss_core -> short Z(N-1)_Core xQ and the prev boss is unbeatable (chained dps wall) â€” LEGIT game gate\n    #  - dps_wall           -> boss_ehp/player_dps > 1800s (game's _offline_winnable threshold);\n    #                          report boss_id, atk by type, boss resists, recommend tier/dmg-type\n    #  - power_wall         -> energy_used>energy_capacity, no affordable higher z{N}_battery\n    #  - ammo_wall          -> weapon equipped but ammo_loadout slot empty/stock==0 and no source\n    #                          (esp. MissileT1: combustion unresearched or craft_missile_t1 unrunnable)\n    #  - slot_wall          -> get_used_slots()>=get_max_slots() and a gate mat can't be stored\n    #  - starvation         -> a needed gather/recipe input has no unlocked source\n    emit {\"evt\":\"STALL\",\"scenario\",\"zone\":N,\"reason\":reason,\"sim_s\",\"step\",\"snapshot\": full milestone snapshot}\n    record stall in summary; ABORT this scenario (move to next) â€” finding, not fatal\n```\nBOT-BUG vs GAME-FINDING: if `_classify_stall` finds an unblock that IS affordable/available but the policy never took it (e.g. recipe_runnable true but never started, or a weapon equipped with stock>0 but set_slot_ammo never called), tag `\"suspect\":\"bot_policy\"`; otherwise `\"suspect\":\"game_gate\"`. The boss-core farm requirement is a LEGIT game-finding worth surfacing: clearing zone N+1 requires re-farming zone N's boss `ceil(cost_items[Z{N}_Core]*MATERIAL_MULTIPLIER)` times (2..10), so each gate's true cost = (1 clear kill + 2..10 farm kills) â€” the bot quantifies this per zone (core_farm_per_zone). Repair-loop deaths = repeated in_combat->false with no core after MAX_DEATHS_PER_FIGHT and `_combat_ready` false => dps/power wall finding, not a crash. HARD_GLOBAL_CAP bounds CI; hitting it is itself a reported finding.

========== CODE_OUTLINE ==========
FILES (all absolute under C:/Users/gokbe/Documents/horizonidle):\n- scripts/sim/progression_bot.gd  (runner, extends Node)\n- scripts/sim/policies/progression.gd  (policy, extends \"res://scripts/sim/policy_base.gd\")\n- scenes/progression_bot.tscn  (root Node -> progression_bot.gd)\n\nprogression_bot.gd:\n```\nextends Node\nconst DT := 0.25\nconst HARD_GLOBAL_CAP := 4_000_000\nconst ZONE_KEY := {1:\"lunar_orbit\",2:\"asteroid_belt\",3:\"mars_debris\",4:\"cryofield\",5:\"sector_alpha\",6:\"sector_beta\",7:\"sector_gamma\",8:\"sector_delta\",9:\"sector_zeta\",10:\"sector_epsilon\"}\nconst POLICY := preload(\"res://scripts/sim/policies/progression.gd\")\nvar sim_s := 0.0\nvar step := 0\nvar _save_bak := \"\"  # backed-up save bytes\nfunc _ready(): call_deferred(\"_boot\")\nfunc _boot():\n    GameState.set_process(false)\n    _backup_save()                                  # warp execute_warp() writes savegame.json â€” protect user save\n    var scen = _arg(\"scenario\", \"\")                 # OS.get_cmdline_user_args(); \"\" -> run all\n    var scens = [scen] if scen != \"\" else [\"A\",\"B\",\"C\"]\n    for sc in scens: _run_scenario(sc, _warps_for(sc), 1)   # A=0,B=1,C=2\n    _restore_save()\n    get_tree().quit()\nfunc _run_scenario(sc, target_warps, run_seed):\n    GameState.hard_reset(); seed(run_seed)\n    GameState.combat_manager.boss_kills.clear()      # CRITICAL: reset() does NOT clear these\n    GameState.combat_manager.total_kills = 0\n    GameState.game_settings[\"offline_combat\"] = false\n    _assert_clean(sc)                                # lifetime_credits==0, boss_kills empty, total_warps==0, no Z*_Core\n    var p = POLICY.new(); p.target_warps = target_warps\n    sim_s = 0.0; step = 0\n    var clears = {}; var stall = null\n    while true:\n        p.manage_meta()\n        if p._should_warp(): _do_warp(p, sc)\n        var N = p._next_uncleared_zone()\n        if N > 10: _emit_summary(sc, \"Z10_CLEARED\", clears, null); return\n        var d = p.decide_session()\n        var res = _run_session(d, p, N)\n        if res == \"CLEARED\": clears[N] = {\"sim_s\":sim_s, \"steps\":step}; _emit_clear(sc, N, p)\n        stall = _watchdog(sc, N, p)\n        if stall != null or step > HARD_GLOBAL_CAP:\n            _emit_summary(sc, \"STALLED\", clears, stall if stall != null else _cap_stall(N)); return\nfunc _run_session(d, p, N):\n    var kind = String(d.get(\"kind\",\"noop\"))\n    if kind == \"combat\":\n        return _run_boss_fight(d[\"zone\"], d[\"enemy\"], d.get(\"length\",60), p, N, int(d.get(\"farm_to\",1)))\n    elif kind == \"noop\":\n        if GameState.combat_manager.in_combat: GameState.combat_manager.retreat()\n        _jump_offline(d.get(\"gap\", 600.0)); return null\n    else:                                            # gather|process\n        _set_active(d[\"mgr\"]); d[\"mgr\"].start_action(d[\"id\"])   # start_action returns NO value (pre-checked)\n        var steps = int(d.get(\"length\",30) / DT)\n        for _i in steps:\n            GameState.infrastructure_manager.process_tick(DT)\n            GameState.bounty_manager.process_tick(DT)\n            GameState.warp_manager.process_charge(DT)\n            if GameState.active_manager: GameState.active_manager.process_tick(DT)\n            sim_s += DT; step += 1\n        return null\nfunc _run_boss_fight(zid, boss_id, length, p, N, farm_to): ...   # see combat_drive VERBATIM (pre-check, pin-assert, loop, retreat)\nfunc _set_active(mgr):                               # match sim_runner._set_active (sim_runner.gd:294-297)\n    if GameState.active_manager and GameState.active_manager != mgr: GameState.active_manager.stop_action()\n    GameState.active_manager = mgr                  # direct assign (combat start_expedition still toasts internally; harmless headless)\nfunc _do_warp(p, sc):                               # see warp_scenarios (execute_warp + branch-aware _spend_shards + logs); force fresh re-arm next manage_meta\nfunc _jump_offline(gap): GameState.process_offline_progress(min(gap, 86400.0)); sim_s += gap\nfunc _backup_save()/_restore_save()/_assert_clean()/_arg()/_warps_for()/_watchdog()/_emit_*: ...\n```\n\nprogression.gd (extends policy_base.gd â€” REUSE is_armed/unlocked_zone_ids/unlocked_gather_actions/unlocked_recipes/recipe_runnable/best_gather/best_recipe_id/sell_surplus/try_unlock_research/unlock_toward/buy_buildings/maybe_upgrade_storage):\n```\nvar target_warps := 0\nvar cleared := {}                              # {tier:int -> true}; set on first boss_kills delta â€” NOT live core inventory\nfunc _next_uncleared_zone() -> int:            # first tier 1..10 not in cleared\nfunc manage_meta() -> void:                    # research unlock_toward(zone_N_access); _arm_for_zone(N); _ensure_powered(N);\n                                               #   _ensure_ammo(N); buy_buildings(1); maybe_upgrade_storage(); repair if hp low & not in combat\nfunc decide_session() -> Dictionary:           # see play_policy pseudo-code (kind-keyed, boss-core farm branch, material/credit branches)\nfunc _should_warp() -> bool:                    # see warp_scenarios\nfunc _arm_for_zone(N):                          # boss = zones[ZONE_KEY[N]].enemies[4]; weakest = argmin(resist_k,e,x);\n    #   kinetic->z{N}_kinetic, energy->z{N}_energy, explosive->z{N}_missile (NOT z{N}_explosive);\n    #   fall back to best ammo-supported type if MissileT1/combustion unavailable;\n    #   construct_hull if better hull researched; resolve slot_type->slot INDEX over hulls[active_hull].slots;\n    #   craft_module+equip_module BATTERY FIRST (slot idx) then weapon; recalc_stats(); after equip call set_slot_ammo\nfunc _ensure_powered(N):                        # while energy_used>energy_capacity: craft+equip higher z{N}_battery\nfunc _ensure_ammo(N):                           # per non-cryo weapon slot: stock matched ammo (SlugT1/CellT1/MissileT1) AND set_slot_ammo(idx,id)\nfunc _combat_ready(N) -> bool:                  # is_armed() and energy_used<=energy_capacity and every non-cryo slot has compatible ammo_loadout entry w/ stock>0\nfunc _gate_shortfall(N) -> Dictionary:          # zone_N_access cost: credits*COST_MULTIPLIER + items*MATERIAL_MULTIPLIER, minus on-hand (incl Z(N-1)_Core)\nfunc _classify_stall(N) -> String:              # research:material|credits|boss_core / dps_wall / power_wall / ammo_wall / slot_wall / starvation\n# helpers: _recipe_producing(sym), _gather_for_recipe_input(rid), _is_refinable(sym), _protected() (boss cores + gate items), _tier_of(zid)\n```\nBoss ids (verified, index 4): z1_boss_architect, z2_boss_monolith, z3_boss_warmaster, z4_boss_overseer, z5_boss_harbinger, z6_boss_colossus, z7_boss_sovereign, z8_boss_warden, z9_boss_patient_zero, z10_boss_leviathan. VERIFIED boss weak-types (data-driven, source of truth): Z1/Z2/Z5/Z8 explosive, Z3/Z6/Z9 energy, Z4/Z7/Z10 kinetic.\nGDScript typing: use explicit `var d: Dictionary = GameState.combat_manager.zones[zid]` (NOT `:=` off untyped GameState.*); tech_tree cost_items are float dicts. Launch: in tools/run_sim.ps1 set $proj=\"C:/Users/gokbe/Documents/horizonidle\", $godot=\"C:/Users/gokbe/AppData/Local/Temp/godot_check/Godot_v4.5.1-stable_win64_console.exe\" (binary verified present), $Scene=\"res://scenes/progression_bot.tscn\" (create it); keep the project.godot backup/restore in finally; run `godot --headless --path <proj> -- --scenario=A` (or omit to run A/B/C).

========== RISKS ==========
RISKS / KNOWN PITFALLS:\n1) ACCEL-TIME vs COMBAT CORRECTNESS â€” #1 risk. Large delta silently breaks combat (one shot/tick, overshoot discarded â€” verified l.1574) and skilling (one action/tick). DT<=0.25 for ALL live combat + accuracy-sensitive skilling; offline jumps ONLY for pure gather/process/infra. combat calculate_offline NEVER spawns bosses (flat ~10s/kill current-enemy sweep, no boss_core, no boss_kills â€” verified l.2800+) so using it for clears yields FALSE 'cleared'. Mitigation: bosses exclusively via the live DT loop with boss_kills-delta detection.\n2) STALE boss_kills/total_kills BETWEEN SCENARIOS â€” NEW verified finding the verifiers under-stated: `combat_manager.reset()` calls only `super.reset()` (Skill.reset just decays xp + resets level, skill.gd:108-112) + `retreat()`; it does NOT clear `boss_kills`/`total_kills` (combat_manager.gd:2751-2753). So hard_reset does NOT zero them, and the WIN-delta signal would mis-fire in scenario B/C against A's leftover kills. Mitigation: explicit `combat_manager.boss_kills.clear(); total_kills=0` at every scenario start + `_assert_clean` that hard-warns and aborts if dirty.\n3) POLICY GETS IT WRONG -> FALSE STALL â€” if decide_session picks a dominated action (gathers an unneeded symbol, never re-issues start_action after a processing self-stop, or never calls set_slot_ammo), the watchdog flags a stall that is a bot bug. Mitigation: _classify_stall tags suspect=bot_policy vs game_gate by checking whether an affordable unblock existed but wasn't taken.\n4) GDSCRIPT TYPING â€” `:=` off untyped GameState.* dynamic returns infers Variant; use explicit `var d: Dictionary = GameState.combat_manager.zones[zid]`. tech_tree cost_items are float dicts (compare with *MATERIAL_MULTIPLIER as float). start_action returns NO value â€” never `if mgr.start_action(id):`. Combat entry toasts internally via set_active_manager regardless of direct assignment (harmless headless); still call prev.stop_action() so a prior skilling loop halts.\n5) RUNTIME LENGTH â€” DT-stepped combat for 10 zones Ã— 3 scenarios + boss-core farming (2..10 extra kills/gate) is millions of process_tick calls; minutes-to-tens-of-minutes headless. HARD_GLOBAL_CAP bounds it; idle stretches MUST use offline jumps; keep combat session length tight (60s clear, 120s farm) and re-decide.\n6) SAVE-STATE I/O â€” execute_warp() unconditionally calls GameState.save_game() (warp_manager ~l.224), overwriting user://savegame.json mid-run despite set_process(false). Mitigation: back up savegame.json+.bak at _boot, restore at quit. Do NOT claim 'no save I/O'.\n7) AMMO/POWER/DEATH EDGES â€” unpowered or 0-ammo weapons do 0 dmg SILENTLY (only a dps wall); set_target_enemy bails BEFORE in_combat on power/hp fail leaving a RANDOM-enemy fight; handle_module_defeat destroys 1/6 modules + clears ammo on death. Mitigation: pre-check power+hp before the fight, ASSERT current_enemy.id==boss_id after set_target_enemy (else classify power_wall), run _ensure_powered/_ensure_ammo (with set_slot_ammo) in manage_meta AND after every re-arm, cap deaths per fight. Watchdog checks energy_used>cap and ammo_loadout-slot-empty explicitly, never infers from low damage.